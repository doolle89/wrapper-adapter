# WrapperRecycleViewAdapter
RecycleView adapter that wraps around any other RecycleView.Adapter and inject its 
elements to specified positions without having any knowledge about the original 
(wrapped) adapter